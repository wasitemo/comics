export default function PrivateAuthDetailPage({ params }: { params: { id: string } }) {
  return (
    <div>
      <h1>Auth Detail</h1>
      {/* TODO: Display auth detail by ID */}
    </div>
  );
}
