export default function PrivateBlogDetailPage({ params }: { params: { id: string } }) {
  return (
    <div>
      <h1>Blog Detail</h1>
      {/* TODO: Display blog detail by ID */}
    </div>
  );
}
