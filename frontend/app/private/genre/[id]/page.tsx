export default function PrivateGenreDetailPage({ params }: { params: { id: string } }) {
  return (
    <div>
      <h1>Genre Detail</h1>
      {/* TODO: Display genre detail by ID */}
    </div>
  );
}
